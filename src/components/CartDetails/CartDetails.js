import React from 'react';

const CartDetails = ({details}) => {
    
    // console.log(details);

    return (
        <div>
            <p>{details.name}</p>
        </div>
    );
};

export default CartDetails;